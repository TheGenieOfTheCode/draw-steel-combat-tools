{
  "name": "DSCT: Peek",
  "type": "script",
  "author": null,
  "img": "icons/svg/eye.svg",
  "scope": "global",
  "command": "// Lean a little way around a corner without giving up the square you are standing in.\n//\n// Run it with one token selected and it offers every adjacent space that token could\n// legally step into: nothing occupying it, no wall in the way, and no cutting a corner\n// past one. Pick a space and the token shifts 15% of a square towards it and picks up a\n// Peeking effect.\n//\n// Run it again while Peeking and the token drops the effect and settles back into the\n// square it is still mostly standing in. No picker the second time.\n//\n// Two things end a peek without running this at all, both handled by the module rather\n// than here, since a macro is not running when they happen:\n//   moving the token to a different square drops the effect and leaves it where you put it\n//   ending its turn while peeking settles it back and drops the effect\nconst M = 'draw-steel-combat-tools';\nconst api = game.modules.get(M)?.api;\nif (!api) { ui.notifications.error('Draw Steel: Combat Tools is not active.'); return; }\n\n// How far into the next square to lean, as a fraction of one square. Tune this freely.\n//\n// Hard capped at 0.49, because settling back looks at the square the token is mostly in.\n// At half a square or more it would be mostly in the space it peeked towards, and running\n// Peek again would drop it there instead of bringing it home: a free step, not a peek.\nconst SHIFT = Math.min(0.49, 0.15);\n\n// Deliberately far slower than the distance warrants. A token creeping a sixth of a square\n// over the better part of two seconds is the entire joke.\nconst LEAN_MS = 1800;\n\nconst GS = canvas.grid.size;\n\nconst controlled = canvas.tokens.controlled;\nif (controlled.length !== 1) {\n  ui.notifications.warn(controlled.length\n    ? 'Peek works from one token at a time. Select just the one doing the looking.'\n    : 'Select the token doing the looking.');\n  return;\n}\n\nconst tok = controlled[0];\nconst act = tok.actor;\nif (!act) { ui.notifications.warn('That token has no actor to carry the effect.'); return; }\n\n// Rounding the top-left to the nearest cell is what \"the square it is mostly in\" means:\n// a shift smaller than half a square always rounds back to where it started.\nconst settle = (doc) => canvas.grid.getTopLeftPoint(\n  canvas.grid.getOffset({ x: doc.x + GS / 2, y: doc.y + GS / 2 }),\n);\n\nconst leanTo = (x, y) => tok.document.update({ x, y }, {\n  animation: { duration: LEAN_MS },\n  pan: false,\n});\n\n// The module owns settling back, so this macro, the hook that fires on a manual move,\n// and the one at the end of a turn all take the same path.\nif (act.effects.find(e => e.getFlag(M, 'peek'))) {\n  await api.peek.unpeek(tok);\n  ui.notifications.info(`${tok.name} stops peeking.`);\n  return;\n}\n\nconst backend = CONFIG.Canvas.polygonBackends.move;\nconst centreOf = (gx, gy) => canvas.grid.getCenterPoint({ i: gy, j: gx });\nconst blocked = (from, to) => !!backend?.testCollision(from, to, { type: 'move', mode: 'any' });\n\nconst origin = canvas.grid.getOffset({ x: tok.document.x + GS / 2, y: tok.document.y + GS / 2 });\nconst og = { x: origin.j, y: origin.i };\nconst w = Math.max(1, Math.round(tok.document.width));\nconst h = Math.max(1, Math.round(tok.document.height));\n\nconst defeated = CONFIG.specialStatusEffects?.DEFEATED ?? 'dead';\nconst occupied = (gx, gy) => canvas.tokens.placeables.some((t) => {\n  if (t.id === tok.id || !t.actor) return false;\n  if (t.actor.statuses?.has(defeated)) return false;\n  const o = canvas.grid.getOffset({ x: t.document.x + GS / 2, y: t.document.y + GS / 2 });\n  const tw = Math.max(1, Math.round(t.document.width));\n  const th = Math.max(1, Math.round(t.document.height));\n  return gx >= o.j && gx < o.j + tw && gy >= o.i && gy < o.i + th;\n});\n\nconst d = canvas.dimensions;\nconst inScene = (gx, gy) => {\n  const c = centreOf(gx, gy);\n  return c.x > d.sceneX && c.x < d.sceneX + d.sceneWidth && c.y > d.sceneY && c.y < d.sceneY + d.sceneHeight;\n};\n\n// The ring of spaces touching the token's own footprint.\nconst ring = [];\nfor (let x = og.x - 1; x <= og.x + w; x++) {\n  for (let y = og.y - 1; y <= og.y + h; y++) {\n    const insideX = x >= og.x && x < og.x + w;\n    const insideY = y >= og.y && y < og.y + h;\n    if (insideX && insideY) continue;\n    ring.push({ x, y });\n  }\n}\n\nconst from = { x: tok.document.x + (w * GS) / 2, y: tok.document.y + (h * GS) / 2 };\n\nconst candidates = [];\nfor (const g of ring) {\n  if (!inScene(g.x, g.y) || occupied(g.x, g.y)) continue;\n\n  const to = centreOf(g.x, g.y);\n  if (blocked(from, to)) continue;\n\n  const dx = Math.sign(g.x - (og.x + (w - 1) / 2));\n  const dy = Math.sign(g.y - (og.y + (h - 1) / 2));\n\n  // Cutting a corner: the two ways round a diagonal both have to be open, matching the\n  // module's own rule for forced movement rather than inventing a second one.\n  if (dx !== 0 && dy !== 0) {\n    const sideA = centreOf(g.x, g.y - dy);\n    const sideB = centreOf(g.x - dx, g.y);\n    if (blocked(from, sideA) || blocked(sideA, to)) continue;\n    if (blocked(from, sideB) || blocked(sideB, to)) continue;\n  }\n\n  candidates.push({ ...g, dx, dy });\n}\n\nif (!candidates.length) {\n  ui.notifications.warn(`${tok.name} has nowhere to peek from. Every space around it is blocked or occupied.`);\n  return;\n}\n\nconst chosen = await new Promise((resolve) => {\n  const layer = new PIXI.Container();\n  layer.eventMode = 'none';\n  canvas.controls.addChild(layer);\n\n  const g = new PIXI.Graphics();\n  layer.addChild(g);\n\n  let hover = null;\n  const paint = () => {\n    g.clear();\n    for (const c of candidates) {\n      const on = hover && hover.x === c.x && hover.y === c.y;\n      g.lineStyle(on ? 3 : 2, on ? 0xffe066 : 0x66ccff, on ? 1 : 0.7);\n      g.beginFill(on ? 0xffe066 : 0x66ccff, on ? 0.32 : 0.12);\n      g.drawRoundedRect(c.x * GS + GS * 0.08, c.y * GS + GS * 0.08, GS * 0.84, GS * 0.84, 6);\n      g.endFill();\n    }\n    if (!hover) return;\n    // A stub of where the token would lean, so the 15% reads before committing to it.\n    g.lineStyle(0);\n    g.beginFill(0xffe066, 0.55);\n    g.drawCircle(from.x + hover.dx * GS * SHIFT, from.y + hover.dy * GS * SHIFT, GS * 0.12);\n    g.endFill();\n  };\n\n  const overlay = api.pickerOverlay.begin({\n    title: `${tok.name} peeks around a corner`,\n    status: 'Pick a space to lean towards.',\n    detail: 'Escape or right click to stay put.',\n    holeRects: [\n      { x: tok.document.x, y: tok.document.y, w: w * GS, h: h * GS },\n      ...candidates.map(c => ({ x: c.x * GS, y: c.y * GS, w: GS, h: GS })),\n    ],\n    showConfirm: false,\n    showCancel: true,\n    onCancel: () => done(null),\n  });\n\n  const atEvent = (event) => {\n    const p = typeof event?.getLocalPosition === 'function' ? event.getLocalPosition(canvas.app.stage)\n      : event?.data?.getLocalPosition ? event.data.getLocalPosition(canvas.app.stage)\n      : canvas.app.renderer.events.pointer.getLocalPosition(canvas.app.stage);\n    const o = canvas.grid.getOffset(p);\n    return candidates.find(c => c.x === o.j && c.y === o.i) ?? null;\n  };\n\n  const onMove = (event) => { hover = atEvent(event); paint(); };\n  const onDown = (event) => {\n    if (event.target?.closest?.('#dsct-picker-topbar')) return;\n    if (event.button === 2) { event.preventDefault(); done(null); return; }\n    if (event.button !== 0) return;\n    const pick = hover;\n    if (!pick) return;\n    event.preventDefault();\n    event.stopPropagation();\n    done(pick);\n  };\n  const onKey = (event) => { if (event.key === 'Escape') { event.preventDefault(); done(null); } };\n  const onContext = (event) => event.preventDefault();\n\n  function done(result) {\n    canvas.stage.off('pointermove', onMove);\n    window.removeEventListener('pointerdown', onDown, true);\n    window.removeEventListener('keydown', onKey, true);\n    window.removeEventListener('contextmenu', onContext, true);\n    layer.parent?.removeChild(layer);\n    layer.destroy({ children: true });\n    overlay.end();\n    resolve(result);\n  }\n\n  canvas.stage.on('pointermove', onMove);\n  window.addEventListener('pointerdown', onDown, true);\n  window.addEventListener('keydown', onKey, true);\n  window.addEventListener('contextmenu', onContext, true);\n  paint();\n});\n\nif (!chosen) return;\n\nconst home = settle(tok.document);\nawait leanTo(home.x + chosen.dx * GS * SHIFT, home.y + chosen.dy * GS * SHIFT);\n\nawait act.createEmbeddedDocuments('ActiveEffect', [{\n  name: 'Peeking',\n  img: 'icons/svg/eye.svg',\n  origin: act.uuid,\n  description: '<p>Leaning out of cover to see past a corner. Run the Peek macro again to settle back.</p>',\n  flags: { [M]: { peek: { x: home.x, y: home.y } } },\n}]);\n\nui.notifications.info(`${tok.name} peeks.`);\n",
  "folder": "DSCTfoldUT000001",
  "sort": 0,
  "ownership": {
    "default": 0
  },
  "flags": {},
  "_stats": {
    "systemId": "draw-steel",
    "coreVersion": "14"
  },
  "_id": "DSCTpeekMacro001",
  "_key": "!macros!DSCTpeekMacro001"
}
